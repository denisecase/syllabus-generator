# ============================================================
# .github/workflows/links.yml (Lychee Link Checker)
# ============================================================
# WHY: Calls org-level link checker; maintained centrally in denisecase/.github.

name: Check Links

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 2 * *"

permissions:
  contents: read
  issues: write

jobs:
  links:
    uses: denisecase/.github/.github/workflows/links.yml@main
